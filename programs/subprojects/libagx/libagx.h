#ifndef LIBAGX_H
#define LIBAGX_H

// This file only exists to allow Meson to create the top-level libagx directory, 
// since it refuses to create intermediate directories when installing headers for
// libagx/font/ and libagx/lib/ to the sysroot.

#endif
